all:
  children:
    # CATEGORY: Management (Control Plane)
    vcenter:
      hosts:
        vcenter01:
          ansible_host: "vc01.vt9labs.local"   # This matches group_vars/vcenters.yml
          ansible_connection: local
    cohesity:
      hosts:
        cohesity01:
          ansible_host: "backup.vt9labs.local" # This matches group_vars/backup.yml
          ansible_connection: local


    # CATEGORY: Physical Infrastructure
    hypervisors:
      hosts:
        esx01.vt9labs.local:
        esx02.vt9labs.local:
        esx03.vt9labs.local:


    # CATEGORY: Guest Operating Systems
    workloads:
      children:
        linux_vms:
          hosts:
            