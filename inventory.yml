all:
  hosts:
    localhost:
      ansible_connection: local

  children:
    vcenter:
      hosts:
        vcenter1:
          ansible_host: "vc01.vt9labs.local"  # Your vCenter FQDN or IP
          ansible_connection: local
